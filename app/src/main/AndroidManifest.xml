<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.ib.kotlindb">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KotlinDB">
        <activity android:name=".SignUpActivity"/>
        <activity android:name=".AccountDashboard" />
        <activity android:name=".AnalyzeINCI" />
        <activity android:name=".ListActivity" /> <!-- <activity android:name=".MainActivity"></activity> -->
        <activity android:name=".MainActivity" /> <!-- <activity android:name=".SignInActivity" /> -->
        <activity
            android:name=".SignInActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.KotlinDB.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>